# Tests for OrangeHrm

## Prepare


### Local recommended tools:


-   VSC
-   Git
-   Node >16


### insolation and setup


-   (optional) install VSC recommended plugins
-   install dependencies: `npm install`
-   setup Playwright with: `npx playwright install --with-deps chromium`